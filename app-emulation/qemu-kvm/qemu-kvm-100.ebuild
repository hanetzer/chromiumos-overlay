# Dummy package that will be thrown away once the
# dev-util/subversion -> dev-vcs/subversion migration finishes.

EAPI="4"

LICENSE="BSD"
SLOT="0"
KEYWORDS="*"

DEPEND="app-emulation/qemu"
RDEPEND="${DEPEND}"
