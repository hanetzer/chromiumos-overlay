diff -rupN strongswan-4.5.3/src/pluto/plutomain.c strongswan-4.5.3.patched/src/pluto/plutomain.c
--- strongswan-4.5.3/src/pluto/plutomain.c	2011-04-21 11:28:53.000000000 -0700
+++ strongswan-4.5.3.patched/src/pluto/plutomain.c	2011-10-12 19:01:57.930634732 -0700
@@ -702,7 +702,9 @@ int main(int argc, char **argv)
 	char buf[1024];
 
 		if (getpwnam_r(IPSEC_USER, &passwd, buf, sizeof(buf), &pwp) != 0 ||
-				pwp == NULL || setuid(pwp->pw_uid) != 0)
+				pwp == NULL ||
+				initgroups(pwp->pw_name, pwp->pw_gid) != 0 ||
+				setuid(pwp->pw_uid) != 0)
 		{
 			plog("unable to change daemon user");
 			abort();
