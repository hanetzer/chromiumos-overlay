# -*- mode: python -*-
# Copyright (c) 2011 The Chromium OS Authors. All rights reserved.
# Distributed under the terms of the GNU General Public License v2

# TODO(clchiou) This is just a minimally buildable layout; offsets and
# lengths are hard-coded (they should be acquired from fdt)

# Required fields for pack_firmware_image
ENTRIES = [
  EntryBlob(name='FW_MAIN',
        offset=FIRMWARE_IMAGE_OFFSET,
        length=FIRMWARE_IMAGE_LENGTH,
        flags=FMAP_AREA_STATIC | FMAP_AREA_COMPRESSED,
        path=U_BOOT_IMAGE),
  EntryKeyBlock(name='VBLOCK',
        offset=VERIFICATION_BLOCK_OFFSET,
        length=VERIFICATION_BLOCK_LENGTH,
        flags=FMAP_AREA_STATIC,
        keyblock=KEYBLOCK,
        signprivate=SIGNPRIVATE,
        version=1,
        fv=U_BOOT_IMAGE,
        kernelkey=KEYDIR + 'kernel_subkey.vbpubk'),
  EntryBlobString(name='FIRMWARE_ID',
        offset=FIRMWARE_ID_OFFSET,
        length=FIRMWARE_ID_LENGTH,
        flags=FMAP_AREA_STATIC,
        value=FWID_STRING),
]
