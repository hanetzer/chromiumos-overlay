Temporary patch to keep polling for battery status when fully charged

--
--- upower-0.9.10/src/linux/up-device-supply.c	2011-04-26 05:18:06.000000000 -0700
+++ upower-0.9.10/src/linux/up-device-supply.c	2011-06-21 15:55:56.000000000 -0700
@@ -548,9 +548,9 @@
 		state = UP_DEVICE_STATE_UNKNOWN;
 	}
 
-	/* only disable the polling if the kernel tells us we're fully charged,
-	   not if we've guessed the state to be fully charged */
-	supply->priv->enable_poll = (state != UP_DEVICE_STATE_FULLY_CHARGED);
+	/* if it's fully charged, continue to poll
+	 * TODO(bleung): Restore previous behavior when battery status changes are reported.
+	 */
 
 	/* reset unknown counter */
 	if (state != UP_DEVICE_STATE_UNKNOWN) {
