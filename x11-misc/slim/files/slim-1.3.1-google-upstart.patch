--- app.cpp	2009/09/25 23:46:09	610
+++ app.cpp	2009/09/27 03:16:11	611
@@ -358,6 +358,9 @@
             LoginPanel->SetName(cfg->getOption("default_user") );
         }
 
+        if (firstloop) {
+            system("/sbin/initctl emit login-prompt-ready &");
+        }
 
         if (!AuthenticateUser(focuspass && firstloop)){
             panelclosed = 0;
